{
  "name": "myownmesh-gui",
  "private": true,
  "version": "0.3.2",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "description": "MyOwnMesh — node-graph UI for the mesh daemon (Tauri + Svelte).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test --test-reporter=tap tests/wire-serialization.test.mjs",
    "tauri": "tauri",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4",
    "@tauri-apps/cli": "^2",
    "svelte": "^5",
    "svelte-check": "^4",
    "typescript": "^5",
    "vite": "^6"
  }
}
