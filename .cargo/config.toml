# Cross-compilation for the NanoKVM SoC (Sophgo SG2002 / T-Head C906, riscv64).
#
# The daemon ships as a fully static **rv64gc musl** binary, built with
# `cargo-zigbuild` (Zig supplies the C compiler + linker) — NOT the device's
# Sophgo C906 host-tools toolchain. This matters:
#
#   rustc always emits *standard* rv64gc objects for this target. The Sophgo
#   toolchain's gcc defaults to the T-Head **vendor** ISA
#   (rv64imafdcv0p7xthead — draft-0.7 vector + xthead custom ELF attributes).
#   When GNU ld links rustc's objects (and Rust's bundled self-contained musl
#   `libc.a`) with that toolchain's vendor-attributed crt/libgcc, it cannot
#   reconcile the arch attributes and dies with
#   "failed to merge target specific data of file ... libc.a(...)".
#
#   A standard rv64gc toolchain links cleanly, and a static-musl rv64gc binary
#   runs fine on the C906 (which implements the full rv64gc base ISA — the
#   vendor extensions are only needed by code that actually uses them, and the
#   daemon doesn't). Zig is the most reliable way to get that toolchain in CI
#   and on a dev box (no fragile musl-gcc download): `just build-risc` runs
#   `cargo zigbuild`, and `just setup-risc` installs zig + cargo-zigbuild.
#
# This file is inert for normal host builds — it only applies to the
# riscv64gc-unknown-linux-musl target. cargo-zigbuild injects the linker and
# ring's C-compiler/AR via env, so nothing else needs configuring here.
# See docs/NANOKVM.md and .github/workflows/release.yml.

[target.riscv64gc-unknown-linux-musl]
# -u getrandom: force musl's getrandom(2) wrapper into the static link.
#
# The workspace carries two generations of the `getrandom` crate (0.2 via
# rand, 0.4 via uuid). 0.2 declares the libc symbol `getrandom` as a WEAK
# extern (its runtime-availability probe); a weak undefined reference does
# not extract members from a static archive, so musl's getrandom.o is never
# pulled in and the symbol resolves to address 0. getrandom 0.4 then calls
# `libc::getrandom` directly, and LLD materializes its PC-relative call
# against address 0 as `auipc ra,0; jalr ra` — a call that jumps to ITSELF.
# The result on the NanoKVM was an uninterruptible, syscall-free infinite
# spin the first time anything minted a UUIDv4 (webrtc-ice's Agent::new,
# 16 random bytes) — inline on the mesh engine driver, freezing the whole
# engine on every boot. Dynamic-glibc builds (every x86 dev box) resolve
# the symbol at runtime, which is why this never reproduced off-device.
# `-u` makes the reference strong, the archive member gets linked, and
# every caller lands on real code.
rustflags = [
    "-C", "target-feature=+crt-static",
    "-C", "link-arg=-Wl,-u,getrandom",
]

# Cross-compilation for the NanoKVM-Pro SoC (Axera AX630C / 2×Cortex-A53,
# aarch64). Like the riscv64 daemon above, this ships as a fully static
# **aarch64 musl** binary built with `cargo-zigbuild`. The NanoKVM-Pro rootfs
# is glibc (Ubuntu 22.04 aarch64), so a static-musl binary runs on it without
# depending on the device's glibc version — the same appliance philosophy as the
# riscv64 build, and immune to CI-runner/rootfs glibc drift.
#
# None of the riscv64 vendor-ISA linker pain applies here: aarch64 has no
# vendor-attribute merge conflict, so a standard musl toolchain links cleanly.
# We still use Zig purely for toolchain-free CI parity with the riscv64 job.
#
# The `-u getrandom` guard IS required, though: it is a static-musl hazard, not
# a riscv64 one. The workspace carries two `getrandom` generations (0.2 weak
# extern, 0.4 direct call); on any static-musl target the weak undefined
# reference fails to pull musl's getrandom.o from the archive, resolving the
# symbol to address 0 and spinning forever on the first UUIDv4. See the detailed
# explanation on the riscv64 target above.
[target.aarch64-unknown-linux-musl]
rustflags = [
    "-C", "target-feature=+crt-static",
    "-C", "link-arg=-Wl,-u,getrandom",
]
